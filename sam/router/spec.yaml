paths:
  /hello:
    get:
      operationId: helloBuffered
      x-target-lambda: ${LPR_SAMPLE_BUFFERED_LAMBDA_ARN}
      x-lpr:
        max_wait_ms: 10
        max_batch_size: 16
        timeout_ms: 2000
        invoke_mode: buffered

  /hello-stream:
    get:
      operationId: helloStream
      x-target-lambda: ${LPR_SAMPLE_STREAMING_LAMBDA_ARN}
      x-lpr:
        max_wait_ms: 10
        max_batch_size: 16
        timeout_ms: 2000
        invoke_mode: response_stream

